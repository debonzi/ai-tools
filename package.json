{
  "name": "dbz-ai-tools-workspace",
  "private": true,
  "type": "module",
  "description": "Workspace coordinator for independently published DBZ Pi packages.",
  "author": "debonzi",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test:python && npm run test:extensions && npm run test:structure && npm run test:shell",
    "test:python": "python3 -m unittest discover -s packages/dbz-skills/skills/dbz-issues/tests -v && python3 -m unittest discover -s packages/dbz-crew/skills/dbz-crew/tests -v",
    "test:extensions": "TZ=UTC node --test packages/pi-codex-usage/agents/pi/extensions/codex-usage/core.test.ts packages/pi-codex-usage/agents/pi/extensions/codex-usage/index.test.ts packages/dbz-crew/agents/pi/extensions/dbz-crew-events/index.test.ts",
    "test:structure": "python3 tests/test_package.py -v",
    "test:shell": "tests/test-package-install.sh",
    "pack:check": "npm pack --dry-run --ignore-scripts --workspaces",
    "check": "npm test"
  },
  "devDependencies": {
    "@changesets/cli": "2.29.8"
  },
  "packageManager": "npm@12.0.1"
}
