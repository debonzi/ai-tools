---
"@debonzi/db11-crew": none
---

Restore `@debonzi/db11-crew` as the source-only 0.2.0 local Linux package for Scout, Planner, and Builder orchestration. Use only the canonical state root `~/.local/state/db11-crew`, including its lazily materialized managed runtime subtree, marker identity `db11-crew`, and Builder ref pattern `refs/heads/db11-crew/<run-id>`, with read-only diagnosis, explicit activation, and exact-session reload kept as separate lifecycle boundaries.
