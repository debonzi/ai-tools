# @debonzi/db11-skills

## 0.1.0

- Rename the former `@debonzi/dbz-skills` package as `@debonzi/db11-skills` with `db11-issues` and `db11-spec` resources.
- Require an explicit consumer migration without temporary DBZ skill aliases.

## Historical releases of `@debonzi/dbz-skills`

The released entry below is preserved verbatim from the former package line.

## 0.1.0

- Establish the first independent package line for the `dbz-issues` and `dbz-spec` skills.
- Keep the Python issue-management CLI bundled with `dbz-issues` and distribute both skills as one multi-skill catalog.
