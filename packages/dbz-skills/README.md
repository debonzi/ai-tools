# @debonzi/dbz-skills

Portable Pi skills for local issue management and implementation-ready specification workflows.

This package provides:

- `dbz-issues`, including its bundled Python CLI;
- `dbz-spec`, including its OpenAI agent metadata.

Install the complete package with `pi install npm:@debonzi/dbz-skills`.
